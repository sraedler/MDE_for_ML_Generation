from sklearn.model_selection import train_test_split
X=${new_name}[${feat_x}]
y=${new_name}.${pred_y}
${split_name}_train_X, ${split_name}_test_X, ${split_name}_train_y, ${split_name}_test_y = train_test_split(X, y,random_state = 0, train_size=${split})